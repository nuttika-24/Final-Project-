3. Technology Stack Justification (เลือกใช้ Visual Studio Code)

🛠 ภาษาโปรแกรมที่เลือกใช้: Python
เหตุผล
- Python เป็นภาษาที่อ่านง่าย เหมาะกับนักศึกษามหาวิทยาลัย
- มีโครงสร้างเรียบง่าย ทำให้พัฒนาโปรแกรมเครื่องคิดเลขได้รวดเร็ว
- มีไลบรารี GUI เช่น Tkinter ที่ใช้งานง่ายและมาพร้อม Python
- เหมาะสำหรับระบบขนาดเล็กที่ไม่ซับซ้อน

🧰 Framework / GUI Library: Tkinter
เหตุผล
- เป็นไลบรารี GUI ที่ติดมากับ Python โดยไม่ต้องติดตั้งเพิ่ม
- สร้างปุ่ม (Button), หน้าจอ (Label), และช่องแสดงผล (Entry) ได้ง่าย
- เหมาะกับระบบเครื่องคิดเลขที่ต้องมีปุ่มตัวเลขและปุ่มเครื่องหมาย
- โครงสร้างโปรแกรมไม่ซับซ้อน เหมาะกับงานนักศึกษาปี 1–2

💻 เครื่องมือพัฒนา (IDE / Editor): Visual Studio Code (VS Code)
เหตุผล
- ใช้ฟรี และเป็นมาตรฐานยอดนิยมสำหรับการพัฒนาโปรแกรม
- รองรับ Python โดยติดตั้งส่วนเสริม (Python Extension) จาก Microsoft
- มีฟีเจอร์ช่วยเขียนโค้ด เช่น Auto-complete, Debugger, ตัวจัดการ Virtual Environment
- ใช้งานเบา ไม่ต้องใช้ทรัพยากรสูง
- สามารถรันโปรแกรม GUI เช่น Tkinter ได้โดยตรงจาก VS Code
- มี Terminal ในตัว ทำให้จัดการ environment ได้สะดวก

📦 Database: ไม่ใช้ (Not Required)
เหตุผล
- ระบบเครื่องคิดเลขพื้นฐานไม่ต้องเก็บข้อมูลประวัติหรือข้อมูลผู้ใช้
- ไม่ต้องบันทึกค่าใดๆ ลงสตอเรจ
- ทำให้ระบบง่ายขึ้นและลดความซับซ้อนที่ไม่จำเป็น
